# CustomLanguageTransalatorusing-LLM
Created it using groq, streamlit and langchain, created it using langserve to get generate JSON responses which can be used to create it in other clients as well.
